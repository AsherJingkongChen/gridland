export class Web {
  public canvas?: Application;
  public camera?: Camera;
  public profiler?: Profiler;
  public zone?: Zone;

  public openWorld(worldid: number): void {
    
  }

  public closeWorld(): void {
    
  }
}
